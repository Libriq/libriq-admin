---
import { getLangFromUrl, useTranslations } from "../../i18n/utils";
import Layout from "../../layouts/Layout.astro";
import Threads from "../../components/Threads";
import HomeButtons from "../../islands/HomeButtons";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---
<Layout pageTitle={t("nav.home")}>
  <div class="fixed z-50 w-full h-full flex flex-col justify-center items-center">
    <div class="p-20 mx-auto">
      <div class="w-full flex flex-col mb-10 text-white">
        <h3 class="text-8xl font-bold mb-2 mx-auto">Libriq</h3>
        <p class="text-2xl font-light mb-4 mx-auto">Votre bibliothèque. Vos infos. Instantanément.</p>
        <HomeButtons lang={lang} client:load />
      </div>
    </div>
  </div>
  <div class="w-full h-screen bg-gray-950">
    <Threads
      amplitude={2}
      distance={0}
      enableMouseInteraction={false}
      client:load
    />
  </div>
</Layout>
